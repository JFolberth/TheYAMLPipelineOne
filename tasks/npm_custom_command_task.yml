parameters:
- name: customCommand
  type: string
  default: ''
- name: workingDir
  type: string
  default: ''
steps:
- task: Npm@1
  inputs:
    command: 'custom'
    customCommand: ${{ parameters.customCommand }}
    workingDir: ${{ parameters.workingDir }}
